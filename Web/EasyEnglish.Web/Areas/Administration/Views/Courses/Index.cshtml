@model EasyEnglish.Web.ViewModels.Administration.Courses.CourseListViewModel

@{
    this.ViewData["Title"] = "Courses";
}

<h1>@this.ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Add Course</a>
</p>
<table class="table table-hover table-striped">
    <thead>
        <tr>
            
            <th>
                @Html.DisplayNameFor(model => model.Courses.First().CourseType)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.Courses.First().StudentsCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Courses.First().Students)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Courses.First().Description)
            </th>           
            <th>
                @Html.DisplayNameFor(model => model.Courses.First().TeacherName)
            </th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Courses)
        {
         @*<div class="content">
           <div class="container-fluid">
             <div class="row">
               <div class="col-lg-12">
                 <div class="card">
                    <div class="card-header">
                    <h5 class="m-0">@item.CourseType</h5>
                    </div>
                   <div class="card-body">
                     <h5 class="card-title">@item.CourseType</h5>
                     <p class="card-text">
                        @item.Students
                     </p>
                     <a asp-action="Details" asp-route-id="@item.Id">Details</a> 
                     <a asp-action="AddStudent" asp-route-id="@item.Id">Add Student</a>
                   </div>
                 </div>
               </div>
             </div>
           </div>
         </div>*@
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourseType)
            </td>                
            <td>
                @Html.DisplayFor(modelItem => item.StudentsCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Students)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TeacherName)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">Details</a>
            </td>
            <td>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>
<partial name="_PagingPartial" model="@Model" />